-- after commited .env to Github, we updated our .gitignore
    - we can run the code below to remove our .env:
TERMINALi. git rm -r --cached .
VSCODE  i. git add .
VSCODE  i. git commit -m "removing .env file from github"
VSCODE  i. git push

-- we do not have access to GPT-4

1. create chatgpt_example.py
    - file that will connect to API of GPT;
    - function that will ask a question to GPT;
    - function that wil get the response of GPT to our question;

2. pip install openai
    - run the above code on terminal

3. it is necessairily to sign up on openai:
    - access dashboard from platform.openai;
    - create our api_key;

4. pip install python-dotenv

5. we will do the same with groc 

--